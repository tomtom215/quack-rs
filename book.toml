[book]
title = "quack-rs"
authors = ["Tom F."]
language = "en"
multilingual = false
src = "book/src"

[rust]
# Without this mdbook invokes rustdoc at its default edition (2015), where
# `--extern` does not populate the extern prelude and every `use quack_rs::...`
# block fails to resolve. Matches the crate's own edition.
edition = "2021"

[build]
build-dir = "book/book"
create-missing = false

[output.html]
site-url = "https://quack-rs.com/"
git-repository-url = "https://github.com/tomtom215/quack-rs"
git-repository-icon = "fa-github"
edit-url-template = "https://github.com/tomtom215/quack-rs/edit/main/{path}"
additional-css = ["book/theme/custom.css"]
additional-js = ["book/theme/mermaid-loader.js"]
no-section-label = false
cname = "quack-rs.com"

[output.html.search]
enable = true
limit-results = 30
teaser-word-count = 30
use-boolean-and = true
boost-title = 2
boost-hierarchy = 1
boost-paragraph = 1
expand = true
heading-split-level = 3
copy-js = true

[output.html.fold]
enable = false
