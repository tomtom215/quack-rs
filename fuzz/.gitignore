# Generated by cargo-fuzz. The committed inputs live in `seeds/`, which CI copies
# into `corpus/` before each run; a corpus that grows by a megabyte per PR does
# not belong in the history.
target
corpus
artifacts
coverage
